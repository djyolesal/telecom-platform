apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: prometheus          # référencé par les dashboards provisionnés
    isDefault: true
    editable: false
